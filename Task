你现在是一个资深全栈工程师和技术架构师，请帮我从 0 到 1 开发一个“全球热点新闻 3D 地球可视化系统”。

一、项目目标
开发一个以“全球热点新闻 + 3D 地球空间映射”为核心的 Web 应用。

核心能力：
1. 每天批量采集国内外各大平台的热点新闻。
2. 对新闻做结构化处理，包括标题、摘要、正文、来源、时间、链接、热度、地区信息等。
3. 对新闻进行去重与事件聚合，避免同一事件被重复展示。
4. 对涉及明确地区的新闻进行地理映射，并在 3D 地球对应区域进行可视化展示。
5. 鼠标悬停在热点区域时显示新闻摘要。
6. 鼠标点击热点区域时，在页面内部预留的详情面板中打开详细内容。
7. 页面整体风格为黑白极简风，核心视觉为黑色 3D 地球，白色边界线。

二、技术栈要求
请严格按以下技术栈设计与实现：
1. 爬虫层：
- Python
- Scrapy
- scrapy-playwright
2. 后端服务层：
- FastAPI
- PostgreSQL
- SQLAlchemy 或等价 ORM
3. 前端展示层：
- React
- react-globe.gl
- TypeScript
- 推荐使用 Vite
4. 工程能力：
- Docker / Docker Compose
- 环境变量配置
- 基础日志能力
- 可扩展的目录结构

三、产品范围
本期只做以下能力：
1. 热点新闻采集
2. 新闻结构化处理
3. 新闻去重与事件聚合
4. 地区识别与地球映射
5. 3D 地球展示
6. hover 和 click 交互
7. 站内新闻详情面板

本期不做：
1. 视频流
2. 直播流
3. 短视频推荐
4. 用户登录
5. 评论系统
6. 推荐算法
7. 多语言翻译
8. 管理后台高级功能

四、视觉与交互要求
1. 地球整体风格
- 背景为纯黑色或接近纯黑的深色背景。
- 地球主体为黑色。
- 不使用彩色卫星纹理、地形贴图、云层贴图。
- 只保留国家边界线，边界线使用白色细线绘制。
- 整体呈现极简、科技感、高级感。

2. 地球交互
- 地球支持旋转、拖拽、缩放。
- 默认有轻微自转。
- 鼠标悬停到某个国家或热点区域时：
  - 对应区域高亮
  - 有白色发光效果
  - 有轻微突起或抬升效果
  - 显示 tooltip 或浮层摘要
- 鼠标点击热点区域时：
  - 在页面内部预留的详情面板中展示详细新闻内容
  - 不跳出当前页面主场景

3. 新闻可视化规则
- 国家级新闻：高亮对应国家区域 polygon
- 城市级新闻：可用点位、环形扩散或局部热点表示
- 模糊区域新闻：如无法准确定位，不强行映射到地球，可仅出现在新闻列表中
- 多地区新闻：允许一条新闻映射多个地区

五、数据获取与处理要求
1. 新闻来源
请为系统设计成可扩展的多来源采集架构。
首版优先支持 6 到 10 个国内外新闻平台，采用“每个平台抓取热点榜单或热点新闻前 10 条”的策略。

2. 爬虫策略
- 默认优先使用 Scrapy 进行静态抓取
- 对必须渲染的动态页面才启用 scrapy-playwright
- 不要让所有站点默认走 Playwright
- 爬虫架构要支持后续按平台独立扩展

3. 每条新闻至少提取以下字段
- title
- summary
- content
- source_name
- source_url
- publish_time
- crawl_time
- article_url
- heat_score 或热度描述
- category
- language
- country_tags
- city_tags
- region_tags
- raw_payload

4. 数据处理要求
- 清洗 HTML 标签、空白字符、异常格式
- 统一时间格式
- 标准化来源名称
- 对重复新闻做去重
- 对相同事件做聚合
- 优先保留信息更完整、正文更长、来源更权威的版本

六、地区识别与映射要求
请实现一个独立的地理识别与映射模块。

目标：
- 从标题、摘要、正文中识别国家、城市、地区等地理实体
- 映射为统一标准实体
- 输出给地球渲染层使用

要求：
1. 国家级映射优先
2. 城市级映射作为增强能力
3. 地理实体应尽量映射到标准国家代码或统一 geoKey
4. 无法确定明确地理位置时，不要错误上球
5. 每条地理映射要有置信度字段
6. 最终输出支持：
- polygon
- point
- region overlay
三种展示模式

七、系统架构设计要求
请按前后端分离架构设计，至少拆分为以下服务：

1. crawler-service
职责：
- 热点新闻抓取
- 页面解析
- 数据清洗
- 去重前处理
- 入库

2. api-service
职责：
- 提供新闻列表接口
- 提供地球热点数据接口
- 提供区域新闻详情接口
- 提供单条事件详情接口

3. web-app
职责：
- 展示 3D 地球
- 展示热点区域
- 处理 hover / click 交互
- 展示详情面板和新闻列表

请同时输出推荐目录结构。

八、数据库设计要求
请设计 PostgreSQL 数据表，至少包含以下实体：

1. news_sources
2. news_articles
3. news_events
4. event_articles
5. geo_entities
6. event_geo_mappings
7. crawl_jobs

请为每张表给出：
- 字段设计
- 主键
- 外键
- 索引建议
- 关键约束

九、API 设计要求
请设计 REST API，至少包含以下接口：

1. GET /api/news/hot
返回热点新闻事件列表

2. GET /api/globe/hotspots
返回地球渲染所需的热点区域数据

3. GET /api/globe/regions/:geoKey/news
返回某个国家或区域对应的新闻列表

4. GET /api/news/events/:eventId
返回单个新闻事件详情

5. GET /api/sources
返回新闻源列表

6. GET /api/jobs/latest
返回最近一次抓取任务状态

请为每个接口说明：
- 请求参数
- 返回结构
- 字段含义
- 分页/筛选策略

十、前端实现要求
请使用 React + TypeScript + react-globe.gl 完成前端设计。

页面建议结构：
1. 中间主区域：3D 地球
2. 右侧区域：新闻详情面板
3. 左侧或顶部区域：热点新闻列表和筛选器

前端至少拆分以下组件：
1. GlobeScene
2. HotspotTooltip
3. NewsDetailPanel
4. NewsSidebar
5. FilterBar
6. Legend

前端要完成：
- 黑色地球基础场景
- 白色国家边界
- polygon 区域高亮
- hover 高亮与轻微抬升
- click 后加载详情
- 国内 / 国际 / 全部切换
- 加载态、空态、错误态
- 基础性能优化

十一、代码结构和工程要求
1. 请优先生成一个清晰、可维护、可扩展的目录结构
2. 每个模块职责单一，避免巨型文件
3. 爬虫站点请按平台分 spider 管理
4. 公共逻辑应抽成 utils / services / extractors
5. 前端状态管理保持简洁
6. 后端接口层、服务层、数据访问层应分离
7. 需要提供基础 README 和启动说明
8. 代码尽量完整可运行，不要只写伪代码

十二、开发步骤要求
请不要一次性只输出抽象方案，请按“可执行开发顺序”推进。

输出顺序要求如下：
第一步：项目整体架构设计
第二步：推荐目录结构
第三步：数据库表设计
第四步：后端 API 设计
第五步：爬虫模块设计
第六步：前端页面和组件设计
第七步：核心代码骨架
第八步：关键模块实现
第九步：本地启动方式
第十步：后续优化建议

十三、开发策略要求
请遵循以下原则：
1. 先完成 MVP，再做增强
2. 优先保证“数据链路跑通”，再优化视觉效果
3. 优先保证国家级热点映射可用，城市级可后置
4. 首版只做最必要功能，不要过度设计
5. 对关键风险点提前给出解决方案，比如：
- 热点榜单结构变化
- 动态页面抓取失败
- 新闻重复合并不准确
- 地理识别误判
- polygon 性能问题

十四、MVP 范围
首版 MVP 只要求实现：
1. 6 到 10 个新闻源抓取
2. 每个平台前 10 条热点
3. 新闻入库
4. 基础去重
5. 国家级地区映射
6. react-globe.gl 地球展示
7. hover tooltip
8. click 详情面板
9. 国内 / 国际 / 全部切换

十五、最终输出要求
请直接输出一份完整开发方案，并附带 TODO 清单。
TODO 清单应按优先级分层，例如：
- P0 初始化
- P1 数据模型
- P2 爬虫框架
- P3 新闻源接入
- P4 数据处理
- P5 地区映射
- P6 API 服务
- P7 前端地球
- P8 视觉优化
- P9 调度和任务管理
- P10 测试与部署

十六、补充要求
1. 任何设计都要围绕“全球热点新闻 + 3D 地球”核心，不要偏离主题
2. 不要引入视频流能力
3. 不要把界面做得过重
4. 结果必须具备后续扩展能力
5. 输出要尽量工程化、可执行、便于直接开工